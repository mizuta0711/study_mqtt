# 応用コード集

## 🧪 実践的なコード例

この章では、実際に動作する応用的なコード例を紹介します。

---

## 📊 応用例1：複数センサー統合システム

複数の種類のセンサー（温度、湿度、照度）を同時にシミュレートします。

**コードファイル**: [../../mqtt_clients/step5/multi_sensor_publisher.py](../../mqtt_clients/step5/multi_sensor_publisher.py)

### 実行方法

```bash
# センサーシステムを起動
python mqtt_clients/step5/multi_sensor_publisher.py

# 別ターミナルで統合ダッシュボードを起動
python mqtt_clients/step5/multi_graph_dashboard.py
```

### 特徴

- ✅ 温度、湿度、照度を同時に送信
- ✅ 各センサーに適したQoS設定
- ✅ 異常値の検出とアラート
- ✅ センサーステータスの管理

---

## 🎨 応用例2：高度なダッシュボード

複数グラフを同時表示し、統計情報も表示する高機能ダッシュボードです。

**コードファイル**: [../../mqtt_clients/step5/advanced_dashboard.py](../../mqtt_clients/step5/advanced_dashboard.py)

### 実行方法

```bash
python mqtt_clients/step5/advanced_dashboard.py
```

### 特徴

- ✅ 3つのグラフを同時表示
- ✅ 平均値、最大値、最小値の表示
- ✅ 閾値ラインの表示
- ✅ センサーステータスの監視
- ✅ データの自動保存機能

---

## 🌡️ 応用例3：リアルな温度変化シミュレーター

時刻や前回値を考慮した、より現実的な温度シミュレーションです。

**コードファイル**: [../../mqtt_clients/step5/realistic_sensor_publisher.py](../../mqtt_clients/step5/realistic_sensor_publisher.py)

### 主なコード（抜粋）

```python
import math
from datetime import datetime
import random

class RealisticTemperatureSensor:
    def __init__(self, base_temp=25.0):
        self.current_temp = base_temp

    def get_base_temperature(self):
        """時刻に応じた基準温度"""
        hour = datetime.now().hour
        # 正弦波で日内変動
        base = 24.0 + 4.0 * math.sin(math.pi * (hour - 6) / 12)
        return base

    def generate_temperature(self):
        """リアルな温度生成"""
        base = self.get_base_temperature()

        # 基準温度に徐々に近づく
        self.current_temp += (base - self.current_temp) * 0.1

        # ランダムなノイズを追加
        self.current_temp += random.uniform(-0.3, 0.3)

        # 範囲制限
        self.current_temp = max(15.0, min(35.0, self.current_temp))

        return round(self.current_temp, 2)
```

---

## 📝 応用例4：データロガー

MQTTメッセージをSQLiteデータベースに保存します。

**コードファイル**: [../../mqtt_clients/step5/data_logger.py](../../mqtt_clients/step5/data_logger.py)

### 実行方法

```bash
# バックグラウンドで常時ログ記録
python mqtt_clients/step5/data_logger.py
```

### 機能

- ✅ 全センサーデータをSQLiteに保存
- ✅ タイムスタンプ付きで記録
- ✅ 後からデータを分析可能
- ✅ データのエクスポート機能

### データベーススキーマ

```sql
CREATE TABLE sensor_data (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    sensor_id TEXT NOT NULL,
    timestamp TEXT NOT NULL,
    data_type TEXT NOT NULL,
    value REAL NOT NULL
);
```

---

## 🚨 応用例5：アラートシステム

異常値を検出して、アラートを送信・表示するシステムです。

**コードファイル**: [../../mqtt_clients/step5/alert_monitor.py](../../mqtt_clients/step5/alert_monitor.py)

### 実行方法

```bash
python mqtt_clients/step5/alert_monitor.py
```

### アラート条件

| センサー | 条件 | アクション |
|:---|:---|:---|
| 温度 | > 30°C or < 18°C | 高温/低温アラート |
| 湿度 | > 70% or < 30% | 湿度アラート |
| センサー | OFFLINEに変化 | デバイスダウンアラート |

---

## 📊 応用例6：統計分析ツール

収集したデータを分析し、統計情報を表示します。

**コードファイル**: [../../mqtt_clients/step5/statistics_analyzer.py](../../mqtt_clients/step5/statistics_analyzer.py)

### 機能

```python
# データ分析の例
def analyze_data(data):
    """統計分析"""
    import numpy as np

    stats = {
        "平均": np.mean(data),
        "中央値": np.median(data),
        "標準偏差": np.std(data),
        "最大値": np.max(data),
        "最小値": np.min(data),
        "範囲": np.max(data) - np.min(data)
    }

    return stats
```

---

## 🎮 応用例7：リモートコントロール

MQTTでデバイスを遠隔制御するシステムです。

### コマンドPublisher

**コードファイル**: [../../mqtt_clients/step5/remote_controller.py](../../mqtt_clients/step5/remote_controller.py)

```python
# コマンド送信例
client.publish("devices/living-room/commands/set-temp", "25", qos=2)
client.publish("devices/bedroom/commands/power", "ON", qos=2, retain=True)
```

### コマンドSubscriber（デバイス側）

```python
def on_message(client, userdata, msg):
    """コマンド受信"""
    if msg.topic.endswith("/commands/set-temp"):
        target_temp = float(msg.payload.decode())
        print(f"🎯 目標温度を{target_temp}°Cに設定")
        # エアコンの制御など

    elif msg.topic.endswith("/commands/power"):
        power = msg.payload.decode()
        print(f"🔌 電源: {power}")
        # デバイスの電源制御
```

---

## 📈 応用例8：データエクスポート機能

収集したデータをCSVやJSON形式でエクスポートします。

**コードファイル**: [../../mqtt_clients/step5/data_exporter.py](../../mqtt_clients/step5/data_exporter.py)

### CSV形式でエクスポート

```python
import csv
from datetime import datetime

def export_to_csv(data, filename):
    """CSVエクスポート"""
    with open(filename, 'w', newline='', encoding='utf-8') as f:
        writer = csv.writer(f)
        writer.writerow(['Timestamp', 'Sensor ID', 'Type', 'Value'])

        for record in data:
            writer.writerow([
                record['timestamp'],
                record['sensor_id'],
                record['type'],
                record['value']
            ])

    print(f"✅ エクスポート完了: {filename}")
```

### JSON形式でエクスポート

```python
import json

def export_to_json(data, filename):
    """JSONエクスポート"""
    with open(filename, 'w', encoding='utf-8') as f:
        json.dump(data, f, indent=2, ensure_ascii=False)

    print(f"✅ エクスポート完了: {filename}")
```

---

## 🔧 応用例9：設定管理システム

JSON設定ファイルでシステムを柔軟に管理します。

**設定ファイル**: `config.json`

```json
{
  "broker": {
    "host": "localhost",
    "port": 1883,
    "username": null,
    "password": null
  },
  "sensors": [
    {
      "id": "living-room-temp",
      "type": "temperature",
      "location": "リビング",
      "topic": "sensors/living-room/temperature",
      "interval": 1,
      "qos": 0,
      "thresholds": {
        "min": 18.0,
        "max": 30.0
      }
    },
    {
      "id": "bedroom-humid",
      "type": "humidity",
      "location": "寝室",
      "topic": "sensors/bedroom/humidity",
      "interval": 2,
      "qos": 0,
      "thresholds": {
        "min": 30.0,
        "max": 70.0
      }
    }
  ],
  "alerts": {
    "enabled": true,
    "email": false,
    "slack": false,
    "console": true
  }
}
```

**コードファイル**: [../../mqtt_clients/step5/config_based_system.py](../../mqtt_clients/step5/config_based_system.py)

---

## 🎯 応用例10：完全統合システム

上記すべての機能を統合した総合システムです。

**コードファイル**: [../../mqtt_clients/step5/integrated_system.py](../../mqtt_clients/step5/integrated_system.py)

### 起動方法

```bash
# すべての機能を起動
python mqtt_clients/step5/integrated_system.py --config config.json
```

### 機能一覧

- ✅ 複数センサーのシミュレーション
- ✅ リアルタイムダッシュボード
- ✅ データロギング（SQLite）
- ✅ アラートシステム
- ✅ 統計分析
- ✅ データエクスポート
- ✅ 設定ファイル管理

---

## 🚀 実行シナリオ

### シナリオ1：基本的な監視システム

```bash
# ターミナル1: センサー起動
python mqtt_clients/step5/sensor_publisher.py

# ターミナル2: ダッシュボード起動
python mqtt_clients/step5/dashboard_subscriber.py

# ターミナル3: ログ記録
python mqtt_clients/step5/data_logger.py
```

### シナリオ2：複数センサー統合監視

```bash
# ターミナル1: 複数センサー
python mqtt_clients/step5/multi_sensor_publisher.py

# ターミナル2: 統合ダッシュボード
python mqtt_clients/step5/multi_graph_dashboard.py

# ターミナル3: アラートモニター
python mqtt_clients/step5/alert_monitor.py
```

### シナリオ3：完全統合システム

```bash
# 一つのコマンドですべて起動
python mqtt_clients/step5/integrated_system.py --config config.json
```

---

## 💡 カスタマイズのヒント

### 1. 新しいセンサータイプの追加

```python
# 気圧センサーを追加
pressure = round(random.uniform(980, 1030), 1)  # hPa
client.publish("sensors/pressure", pressure, qos=0)
```

### 2. アラート通知先の追加

```python
# Slack通知
import requests

def send_slack_alert(message):
    webhook_url = "YOUR_WEBHOOK_URL"
    requests.post(webhook_url, json={"text": message})

# メール通知
import smtplib

def send_email_alert(message):
    # メール送信処理
    pass
```

### 3. データ分析の追加

```python
# 移動平均
def moving_average(data, window=10):
    return [sum(data[i:i+window])/window for i in range(len(data)-window+1)]

# トレンド検出
def detect_trend(data):
    if len(data) < 5:
        return "不明"
    recent = sum(data[-5:]) / 5
    older = sum(data[-10:-5]) / 5
    if recent > older + 1:
        return "上昇傾向"
    elif recent < older - 1:
        return "下降傾向"
    else:
        return "安定"
```

---

## ✅ 実装チェックリスト

- [ ] 基本的なセンサーPublisherを実装した
- [ ] ダッシュボードSubscriberを実装した
- [ ] 複数センサーを統合した
- [ ] データロギングを実装した
- [ ] アラートシステムを実装した
- [ ] 統計分析を実装した
- [ ] データエクスポートを実装した
- [ ] 設定ファイルで管理できるようにした

---

**関連ドキュメント:**
- [01_IoTシステム構築実践.md](./01_IoTシステム構築実践.md) - 基本実装
- [02_センサーシミュレーション詳細.md](./02_センサーシミュレーション詳細.md)
- [03_データ可視化詳細.md](./03_データ可視化詳細.md)
- [04_実践的な設計パターン.md](./04_実践的な設計パターン.md)
